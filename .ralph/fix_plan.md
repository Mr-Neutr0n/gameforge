# Game Builder — Full Audit & Fix Plan

## Phase 1: Playwright E2E Test Infrastructure
- [x] TASK-001: Install Playwright in frontend (`cd frontend && npm init playwright@latest -- --yes --browser=chromium`), create `playwright.config.ts` with baseURL localhost:3000, create `e2e/` directory. Add `test:e2e` script to package.json. Verify `npx playwright test --list` works.
- [x] TASK-002: Write E2E test for landing page (`frontend/e2e/landing.spec.ts`) — verify page loads, "Game Builder" branding visible in nav and footer, sign-in button present, hero heading renders "Describe a game", 4 example game cards render, "Powered by Gemini + Phaser.js" pill visible, Google and GitHub auth buttons present.
- [x] TASK-003: Write E2E test for 404 page (`frontend/e2e/not-found.spec.ts`) — navigate to `/this-page-does-not-exist`, verify 404 digits visible, controller icon renders, "Page not found" heading visible, "Go Home" link has href="/", "Dashboard" link has href="/dashboard", "Game Builder" footer text present.
- [x] TASK-004: Write E2E test for create page auth guard (`frontend/e2e/create.spec.ts`) — verify navigating to `/create` while unauthenticated redirects to sign-in. Use Playwright route mocking to simulate authenticated session and verify: page title "New Game" visible, 4 template cards render (Platformer, Top-Down, Shooter, Puzzle), textarea placeholder contains "Describe your game", generate button exists and is disabled when textarea is empty.
- [x] TASK-005: Write E2E test for dashboard page (`frontend/e2e/dashboard.spec.ts`) — verify navigating to `/dashboard` while unauthenticated redirects. Mock authenticated session + empty games API response, verify empty state: "No games yet" text, "New Game" button. Mock games API with 2 sample games, verify game cards render with titles, dates, public/private badges.
- [x] TASK-006: Write E2E test for workspace page (`frontend/e2e/workspace.spec.ts`) — Mock auth + game data API responses. Verify: "Game" header text (not "GAME"), empty state shows "Ready to generate" text (not "PRESS START"), generate button visible, activity sidebar has "Activity" heading. Mock game with code, verify iframe container renders, toolbar shows game title, save button present.
- [x] TASK-007: Write E2E test for public share page (`frontend/e2e/share.spec.ts`) — Mock public game API response with game_code. Verify: game title displayed, "Game Builder" header link present, fullscreen button exists, game iframe renders, "Made with Game Builder" text visible, creator name and date shown.

## Phase 2: Backend Hardening
- [x] TASK-008: Fix validator regex brittleness — in `backend/app/agents/tools.py` `validate_phaser_config()`: improve regex to handle ES6 class syntax variants, arrow function scenes, and module patterns. In `backend/app/audits/code_audit.py`: fix `\bvar\s+` regex to handle `var\t` and `var\n`. In `backend/app/audits/logic_audit.py`: improve scene detection regex to handle edge cases. Write unit tests in `backend/tests/test_validators.py` for all fixed patterns.
- [x] TASK-009: Add generation timeout — in `backend/app/routes/generate.py`, wrap the SSE event_stream generator with `asyncio.wait_for()` or `asyncio.timeout()` (5 minute max for generate, 3 min for iterate). On timeout: emit `{"type": "error", "message": "Generation timed out"}` SSE event, set game status="failed", clean up ADK session. Test with existing test suite.
- [x] TASK-010: Fix transaction safety in generate/iterate — in `backend/app/routes/generate.py`, ensure game_code is saved to DB before running audits, so a failing audit doesn't lose generated code. Wrap audit execution in try/except so it can fail independently. Verify with existing tests.
- [ ] TASK-011: Fix session cleanup — in `backend/app/agents/runner.py`, improve `delete_session()` error handling: log the session_id on failure, add a fallback to clear the in-memory dict directly if the service method fails. Add a comment documenting the cleanup strategy.
- [ ] TASK-012: Add backend unit tests — create `backend/tests/test_audits.py` with tests for: (a) logic_audit with valid platformer code (should pass), empty string (should fail gracefully), code missing Phaser.Game (should fail); (b) ui_audit with valid config (should pass), missing width/height, oversized dimensions; (c) code_audit with clean code (should pass), code using eval() (should fail), code using var (should flag). Create `backend/tests/test_tools.py` with tests for validate_phaser_config edge cases.

## Phase 3: Frontend Robustness
- [ ] TASK-013: Add SSE timeout and error handling — in `frontend/src/lib/api.ts`, add: (a) AbortController with 5-minute timeout for streamGameGeneration and streamGameIteration; (b) in the fetch error handler, distinguish between timeout (show "Generation timed out") vs network error (show "Connection lost"); (c) ensure AbortController.abort() is called on component unmount. In workspace page, show appropriate toast messages for each error type.
- [ ] TASK-014: Add long-running generation UI feedback — in workspace page `GamePreviewEmpty` component, when `isGenerating` is true for more than 2 minutes (track with useEffect + elapsed state), show additional text: "Taking longer than expected..." below the loading spinner. This is purely a UI hint, no functional change needed.

## Phase 4: Verification
- [ ] TASK-015: Run full verification — run `cd frontend && npm run build` to confirm zero build errors. Run `cd backend && python -m pytest tests/ -v` to confirm all tests pass (old + new). List any remaining warnings. Create `.ralph/docs/generated/audit-report.md` summarizing: total issues found, issues fixed, test coverage added, remaining known limitations.
